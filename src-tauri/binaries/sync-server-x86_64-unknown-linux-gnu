#!/bin/bash
# Mutter Sync Server Sidecar
# This script is executed by Tauri to start the Automerge sync server

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Start the Node.js sync server
exec node "$SCRIPT_DIR/sync-server.mjs" "$@"
